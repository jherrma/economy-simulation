<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>EconomySimulation.Engine</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="FluentResults" Version="4.0.0" />
  </ItemGroup>

  <!-- The conventions in spec/stories/01-04, enforced by the compiler. ReferenceOutputAssembly
       is false: this is a build-time extension, not something the engine links against. -->
  <ItemGroup>
    <ProjectReference Include="..\EconomySimulation.Analyzers\EconomySimulation.Analyzers.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
